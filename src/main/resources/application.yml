spring:
  datasource:
    url: jdbc:mysql://localhost:8080/HotelBAO
    username: root
    password: MySql@2025!vdK7qPz
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect